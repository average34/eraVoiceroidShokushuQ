;キャラの初期レベルと経験値が見合っていない部分を補正
@CHARA_EXP_INIT
;LOCAL
;0	LOOP
;1	LOOP2

FOR LOCAL:0, 1, CHARANUM
	CALL CHARA_EXP_INIT_SINGLE(LOCAL:0)
NEXT

@CHARA_EXP_INIT_SINGLE(ARG:0)
FOR LOCAL:1, 0, 15
	IF (ABL:(ARG:0):(LOCAL:1) > 0)
		;レベルが１以上だった場合、補正する
		CALL GET_NEED_EXP(LOCAL:1, ABL:(ARG:0):(LOCAL:1))
		EXP:(ARG:0):(LOCAL:1) = RESULT
	ENDIF
NEXT

