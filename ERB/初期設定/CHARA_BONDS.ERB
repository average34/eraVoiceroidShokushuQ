;仲のいいキャラ
@CHARA_BONDS
#DIM LCOUNT

	;キャラ初期設定
	FOR LCOUNT, 1, CHARANUM
		IF CFLAG:LCOUNT:5 >= 120 && CFLAG:LCOUNT:5 > CFLAG:(FLAG:95):5
			FLAG:95 = LCOUNT
		ENDIF
	NEXT
IF FLAG:95
	PRINTFORML %CALLNAME:(FLAG:95)%の声が遠くで聞こえた
	PRINTFORML どうやら%CALLNAME:(FLAG:95)%も異変に巻き込まれたらしい……
ENDIF

