;触手包帯
@SYSTEM_MESSAGE_508(ARG:0, ARG:1, ARGS:0, ARGS:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6)


IF ARG:1 == 0
IF FLAG:AUTOMODE
    PRINTFORML 巻き終わった途端に包帯に擬態した触手が突然うごめき%ARGS:0%の胸に絡みつき始めた！
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  巻き終わった途端に包帯に擬態した触手が突然うごめき%ARGS:0%の胸に絡みつき始めた！
ENDIF

	PRINTFORML 引き剥がそうとすると肩と肘の関節を固めて来る……
	PRINTFORML 無理に引き剥がそうとすると折られるようだ……
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%は諦めて包帯をそのままにした……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%は諦めて包帯をそのままにした……
ENDIF

IF FLAG:AUTOMODE
    PRINTFORML 
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  
ENDIF


ELSEIF ARG:1 == 1
	IF BASE:3 > MAXBASE:3 / 2 && RAND:3 == 0
IF FLAG:AUTOMODE
    PRINTFORML 包帯が%ARGS:0%の服の下でうごめいている……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  包帯が%ARGS:0%の服の下でうごめいている……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 3 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%の巨大な乳房の根本から先端にかけて包帯が巻き付き搾るように絡みついている……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%の巨大な乳房の根本から先端にかけて包帯が巻き付き搾るように絡みついている……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 3 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%の胸を突き出すように固定した上で包帯が巨大な乳房を締め付けてくる……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%の胸を突き出すように固定した上で包帯が巨大な乳房を締め付けてくる……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 1 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%の豊満な胸の根本から先端にかけて包帯が巻き付き搾るように絡みついている……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%の豊満な胸の根本から先端にかけて包帯が巻き付き搾るように絡みついている……
ENDIF

	ELSEIF GET_BUST(TARGET) >= 1 && RAND:2 == 0
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%の胸を突き出すように固定した上で包帯が乳房を締め付けてくる……
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%の胸を突き出すように固定した上で包帯が乳房を締め付けてくる……
ENDIF

	ELSE
IF FLAG:AUTOMODE
    PRINTFORML %ARGS:0%をいじめるように包帯が胸を締め付けてくる…
    TWAIT FLAG:AUTOMODE * 400, 0
ELSE
    PRINTFORMW  %ARGS:0%をいじめるように包帯が胸を締め付けてくる…
ENDIF

	ENDIF
ENDIF

