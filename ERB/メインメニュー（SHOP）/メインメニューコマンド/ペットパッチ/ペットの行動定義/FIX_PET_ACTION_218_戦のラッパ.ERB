;戦のラッパ
@FIX_PET_ACTION_218
;メイン処理回避フラグ　これ立てる場合は全部関数内で処理つくること。
PET:99 = 1
CALL PRINT_MESSAGE(-1, 400)

VARSET LOCAL
SIF !CFLAG:1:29
	LOCAL = RAND:100
SIF FLAG:10 && !CFLAG:(FLAG:10):29
	LOCAL:1 = RAND:100
SIF FLAG:11 && !CFLAG:(FLAG:11):29
	LOCAL:2 = RAND:100

MAX LOCAL, LOCAL:1, LOCAL:2

SIF RESULT == LOCAL
	LOCAL:3 = 1
SIF RESULT == LOCAL:1
	LOCAL:3 = FLAG:10
SIF RESULT == LOCAL:2
	LOCAL:3 = FLAG:11

PRINTFORML %CALLNAME:(LOCAL:3)%は色々な意味で高ぶり始めた！！
CFLAG:(LOCAL:3):28 = MIN(CFLAG:(LOCAL:3):28 + 1, 3)
BASE:(LOCAL:3):4 = MAXBASE:(LOCAL:3):4