;自慰中の攻撃
;CHARA_ID	実行キャラ
@FIX_PT_ACTION_47(CHARA_ID)
#DIM CHARA_ID

TFLAG:51 = CHARA_ID
TFLAG:53 = CFLAG:CHARA_ID:51
TFLAG:55 = 0
IF TALENT:CHARA_ID:151
	TFLAG:56 = 4
ELSE
	TFLAG:56 = 0
ENDIF
CALL GET_EQUIP_BONUS(CHARA_ID, 0)
TFLAG:57 = RESULT + ABL:CHARA_ID:98

TFLAG:58 = 40
;触手服かスライム服で火力が上がる
IF CFLAG:CHARA_ID:37 == 1
	TFLAG:58 += 5
ENDIF
IF CFLAG:CHARA_ID:48 == 1
	TFLAG:58 += 5
ENDIF

TFLAG:61 = 50

TFLAG:68 = 100

CALL PRINT_MESSAGE(CHARA_ID, 1, 47, CALLNAME:CHARA_ID, "")

;強制自慰フラグ
CFLAG:CHARA_ID:156 = 1
CALL MASTURBATION(CHARA_ID, 1, 100)
;強制自慰による絶頂は、終了後EP1に
BASE:CHARA_ID:2 = 1

;1行開けておく
PRINTFORML
