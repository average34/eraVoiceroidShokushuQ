@EXTRA_ACTION_63(CHARA_ID, ENEMY_ID)
#DIM CHARA_ID
#DIM ENEMY_ID
;弾数消費

IF TFLAG:80
	CFLAG:CHARA_ID:17 -= TFLAG:80
ENDIF

IF TFLAG:81
	CFLAG:CHARA_ID:18 -= TFLAG:81
ENDIF

IF TFLAG:82
	CFLAG:CHARA_ID:19 -= TFLAG:82
ENDIF

