;ばら撒き弾
;CHARA_ID	実行キャラ
@FIX_PT_ACTION_14(CHARA_ID)
#DIM CHARA_ID

TFLAG:51 = CHARA_ID
TFLAG:53 = CFLAG:CHARA_ID:51
TFLAG:55 = 0
IF TALENT:CHARA_ID:151
	TFLAG:56 = 4
ELSE
	TFLAG:56 = 0
ENDIF
CALL GET_EQUIP_BONUS(CHARA_ID, 0)
TFLAG:57 = RESULT + ABL:CHARA_ID:98

TFLAG:58 = 25
TFLAG:61 = 75

TFLAG:68 = 100

TFLAG:99 = 59

FLAG:(FLAG:5 + 50) -= RAND:4 + 1
SIF FLAG:(FLAG:5 + 50) < 0
	FLAG:(FLAG:5 + 50) = 0

CALL PRINT_MESSAGE(CHARA_ID, 1, 14, CALLNAME:CHARA_ID, "")


@EXTRA_ACTION_59(CHARA_ID, ENEMY_ID)
#DIM CHARA_ID
#DIM ENEMY_ID
PRINTFORML %SAVESTR:ENEMY_ID%の回避率が一時的に少し下がった！
DA:ENEMY_ID:81 += 5 + RAND:5 + RAND:5
IF DA:ENEMY_ID:81 > 50
	DA:ENEMY_ID:81 = 50
ENDIF

