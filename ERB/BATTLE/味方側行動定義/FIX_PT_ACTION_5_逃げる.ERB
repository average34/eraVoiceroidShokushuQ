;逃走
;CHARA_ID	実行キャラ
@FIX_PT_ACTION_5(CHARA_ID)
#DIM CHARA_ID

;全部こちらで処理するのでメイン処理不要
TFLAG:66 = 1

LOCAL:1 = GET_ESCAPE_RATE(CHARA_ID)

IF RAND:100 >= LOCAL:1 || CFLAG:CHARA_ID:200 > 0
	CALL PRINT_MESSAGE(CHARA_ID, 1, 5, CALLNAME:CHARA_ID, "", 0)
ELSE
	CALL PRINT_MESSAGE(CHARA_ID, 1, 5, CALLNAME:CHARA_ID, "", 1)
	TFLAG:12 = 1
ENDIF

