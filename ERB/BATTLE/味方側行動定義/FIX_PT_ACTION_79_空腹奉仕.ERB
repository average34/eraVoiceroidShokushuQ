;空腹奉仕　行動１００番以降はエラー
;CHARA_ID	実行キャラ
@FIX_PT_ACTION_79(CHARA_ID)
#DIM CHARA_ID

;全部こちらで処理するのでメイン処理不要
TFLAG:66 = 1

CALL PRINT_MESSAGE(CHARA_ID, 1, 79, CALLNAME:CHARA_ID, "")

LOCAL:0 = 200 + RAND:50
CALL ADD_ABL_EXP_15(CHARA_ID, LOCAL:0)

LOCALS:0 = 強制触手奉仕

PRINTFORML %CALLNAME:CHARA_ID, 10, LEFT%：%LOCALS:0, 8, LEFT% EXP：　{EXP:CHARA_ID:15}(+{LOCAL:0})


;●りばーす用処理
IF CHARA_ID == 1 && FLAG:10 > 0
CFLAG:(FLAG:10):5 -= 5
ENDIF

IF CHARA_ID == 1 && FLAG:11 > 0
CFLAG:(FLAG:11):5 -= 5
ENDIF

;相手のテンションを下げる効果を追加
		IF BASE:CHARA_ID:4 > 120
			BASE:CHARA_ID:4 -= 10
		ELSEIF BASE:CHARA_ID:4 < 80
			BASE:CHARA_ID:4 += 10
		ELSE

		ENDIF


