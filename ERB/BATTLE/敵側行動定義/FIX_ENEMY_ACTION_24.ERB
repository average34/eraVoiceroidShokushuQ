@FIX_ENEMY_ACTION_24(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 6
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

TFLAG:58 = 100
TFLAG:59 = 0

TFLAG:61 = 1000

TFLAG:99 = 24

CALL ACTION_FILTER(-1, 7, 24, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54)



@EXTRA_ACTION_24(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

LOCAL:1 = 500

IF CFLAG:1:20 || CFLAG:(FLAG:10):20 || CFLAG:(FLAG:11):20
	BASE:CHARA_ID:4 -= 10
	IF BASE:CHARA_ID:4 < 0
		BASE:CHARA_ID:4 = 0
	ENDIF
	CALL ACTION_FILTER(-1, 17, 24, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54,1)
ELSE
	TFLAG:12 = 1
	CALL ACTION_FILTER(-1, 17, 24, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54,0)

ENDIF


