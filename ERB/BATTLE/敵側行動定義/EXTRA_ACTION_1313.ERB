@EXTRA_ACTION_1313(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率　侵入者有利のときは、技も強化される。

LOCAL:1 = 0

IF FLAG:10 > 0

	LOCAL:1 = 0

ENDIF

IF FLAG:11 > 0

	LOCAL:1 = 0

ENDIF

IF CFLAG:CHARA_ID:29 > 0
	LOCAL:1 = 400
ENDIF


;汚れ追加
IF FLAG:(FLAG:5 + 50) < 40
	FLAG:(FLAG:5 + 50) = 40
ELSEIF FLAG:(FLAG:5 + 50) < 100
	FLAG:(FLAG:5 + 50) += 1
ELSE


ENDIF



IF RAND:100 < LOCAL:1


	BASE:CHARA_ID:3 /= 2
	BASE:CHARA_ID:5 += 100
	CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)


ELSE
	;CALL PRINT_MESSAGE(CHARA_ID, 27, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)


ENDIF
