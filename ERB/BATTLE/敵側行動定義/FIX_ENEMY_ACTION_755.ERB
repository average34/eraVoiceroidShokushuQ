@FIX_ENEMY_ACTION_755(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 3
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

TFLAG:58 = 10000 / TFLAG:57 + 150
TFLAG:61 = 150

TFLAG:64 = 50
TFLAG:65 = 10

TFLAG:59 = 0
TFLAG:90 = 600
TFLAG:91 = 0
TFLAG:92 = 0

TFLAG:63 = 1
TFLAG:99 = 755

CALL ACTION_FILTER(-1, 4, 755, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54, TFLAG:63)

@EXTRA_ACTION_755(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID

;B寄生
;拡張+が必要
IF FLAG:516 > 0 && CFLAG:CHARA_ID:161 == 0 && TALENT:CHARA_ID:162 > 1 && RAND:4 == 0
	CALL PARASITE_TYPE(CHARA_ID, 3, 5 + RAND:12, 4 + RAND:4 + RAND:3, 0)
	PRINTFORML %CALLNAME:CHARA_ID%は何かに【寄生】されてしまった！
ENDIF


