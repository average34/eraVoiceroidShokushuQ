@FIX_ENEMY_ACTION_1055(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 6
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

TFLAG:58 = 100
TFLAG:59 = 0

TFLAG:61 = 80

TFLAG:99 = 1055

CALL ACTION_FILTER(-1, 2, 1055, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54)


@EXTRA_ACTION_1055(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
#DIM CUT_PARTS
;LOCAL
;1	命中率

LOCAL:1 = 120

CUT_PARTS = 0

CALL DAMAGE_COMMON(CHARA_ID, 0, 1500 + RAND:1500)

IF FLAG:507 > 1 && BASE:CHARA_ID:HP <= 0
	CUT_PARTS = RAND:2 + 5
	CALL CUT_LIMBS(CHARA_ID, CUT_PARTS, 30, 1, ENEMY_ID)
	IF RESULT
		CALL PRINT_MESSAGE(CHARA_ID, 476, CUT_PARTS * 10, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
	ENDIF
ELSE
	
ENDIF


