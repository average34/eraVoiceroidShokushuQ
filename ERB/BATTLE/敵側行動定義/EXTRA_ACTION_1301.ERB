@EXTRA_ACTION_1301(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

LOCAL:1 = 40

IF FLAG:10 > 0

	LOCAL:1 += 20

ENDIF

IF FLAG:11 > 0

	LOCAL:1 += 20

ENDIF

IF RAND:100 < LOCAL:1

	CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
	CFLAG:CHARA_ID:301 = 4
ELSE
	CALL PRINT_MESSAGE(CHARA_ID, 27, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)


ENDIF




