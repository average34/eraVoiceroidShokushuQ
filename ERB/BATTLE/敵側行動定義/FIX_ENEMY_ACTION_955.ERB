@FIX_ENEMY_ACTION_955(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 6
TFLAG:60 = 1


TFLAG:61 = 80

TFLAG:99 = 955

CALL ACTION_FILTER(-1, 7, 955, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54)



@EXTRA_ACTION_955(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

LOCAL:1 = 2000

IF FLAG:(FLAG:5 + 50) < 100
	FLAG:(FLAG:5 + 50) = MIN(100, (FLAG:(FLAG:5 + 50)) + 20 + RAND:8)
ENDIF
CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
