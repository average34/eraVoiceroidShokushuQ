@EXTRA_ACTION_1317(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

;衰弱毒

LOCAL:1 = 25

IF FLAG:10 > 0

	LOCAL:1 += 25

ENDIF

IF FLAG:11 > 0

	LOCAL:1 += 25

ENDIF


;毒耐性
IF TALENT:CHARA_ID:56
	LOCAL:1 = 5
ENDIF


IF RAND:100 < LOCAL:1

	CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)

;速度
	CFLAG:CHARA_ID:300 = 4
;回避
	CFLAG:CHARA_ID:302 = 4
ELSE
	CALL PRINT_MESSAGE(CHARA_ID, 27, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)


ENDIF




