@FIX_ENEMY_ACTION_1052(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 1
TFLAG:60 = 1


TFLAG:61 = 120
TFLAG:64 = 10
TFLAG:89 = 2



TFLAG:99 = 1052

CALL ACTION_FILTER(-1, 2, 1052, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54)



@EXTRA_ACTION_1052(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
#DIM CUT_PARTS
;LOCAL
;1	命中率

LOCAL:1 = 120

CUT_PARTS = 0

CALL DAMAGE_COMMON(CHARA_ID, 0, 1200 + RAND:3000)

IF FLAG:507 > 1 && BASE:CHARA_ID:HP <= 0
	CUT_PARTS = RAND:4
	CALL CUT_LIMBS(CHARA_ID, CUT_PARTS, 30, 1, ENEMY_ID)
	IF RESULT
		CALL PRINT_MESSAGE(CHARA_ID, 476, CUT_PARTS * 10, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
	ENDIF
ELSE
	
ENDIF


