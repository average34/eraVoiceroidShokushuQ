@EXTRA_ACTION_1307(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)

;相手絶頂時に相手の最大ＥＰをダウンさせる
IF CFLAG:CHARA_ID:35

	MAXBASE:CHARA_ID:2 -= 500
	IF MAXBASE:CHARA_ID:2 < 5000
		MAXBASE:CHARA_ID:2 = 5000
	ENDIF
	
	;IF FLAG:4 == 3
	;	DA:ENEMY_ID:1 += DA:ENEMY_ID:2 / 5
	;ELSE
		;DA:ENEMY_ID:1 += LIMIT(DA:ENEMY_ID:2 / 5, 0, 10000)
	;	DA:ENEMY_ID:1 += LIMIT(DA:ENEMY_ID:2 / 5, 0, 3000)
	;ENDIF
ELSE



ENDIF

