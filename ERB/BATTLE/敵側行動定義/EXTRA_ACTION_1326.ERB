@EXTRA_ACTION_1326(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率
;水窒息

;命中率チェック　通常攻撃属性
CALL GET_HIT_RATE_ENEMY(ENEMY_ID, CHARA_ID, 1)
LOCAL:1 = RESULT

IF RAND:100 < LOCAL:1
	CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
	LOCAL:2 = MAXBASE:CHARA_ID:0 / 7 * (90 + RAND:20) / 100
	CALL DAMAGE_COMMON(CHARA_ID, 0, LOCAL:2, 0)
	IF BASE:CHARA_ID:0 < 0
		BASE:CHARA_ID:0 = 0
	ENDIF
ELSE
	CALL PRINT_MESSAGE(CHARA_ID, 27, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
ENDIF
