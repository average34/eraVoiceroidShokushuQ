@EXTRA_ACTION_1310(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)

;問答無用で眠くなる

IF RAND:3 == 0
	FLAG:7 += 1
ENDIF

