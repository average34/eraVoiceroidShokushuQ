@FIX_ENEMY_ACTION_563(ENEMY_ID)
#DIM ENEMY_ID

;52が技使用者、54がターゲット。基本的に弄らなくていい
TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51

;55には攻撃種別が入り、1~5がそれぞれ、通常・衣服・性・拘束・寄生
TFLAG:55 = 4
;60が1なら喪失の相手にも効く。基本的に弄らなくていい
TFLAG:60 = 1

;ベース威力。基本的に（ｒｙ
TFLAG:57 = DA:ENEMY_ID:5

;コマンド威力。拘束攻撃だと意味はない
TFLAG:58 = 50
;命中基準値。攻撃種別によって適切な値が違ってくる
TFLAG:61 = 30

;補助値として使われるフラグ。拘束の場合1なら二人同時拘束可能
TFLAG:64 = 0
;補助値フラグその２。0が通常の拘束、1が丸呑みとなる
TFLAG:65 = 1

;59が1なら全体攻撃、91が2以上なら多段攻撃になる
TFLAG:59 = 0
TFLAG:91 = 0

CALL ACTION_FILTER(-1, 5, 563, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54, TFLAG:65)


