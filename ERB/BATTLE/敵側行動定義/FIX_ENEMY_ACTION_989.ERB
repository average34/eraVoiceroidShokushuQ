@FIX_ENEMY_ACTION_989(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 3
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

TFLAG:58 = 90
TFLAG:61 = 120

TFLAG:64 = 50
TFLAG:65 = 10

TFLAG:59 = 0
TFLAG:90 = 20
TFLAG:91 = 0
TFLAG:92 = 63
TFLAG:99 = 989

TFLAG:63 = 9

; 催眠の種類ランダム選出用にキャラ別の催眠種類を指定しておく
SIF !CFLAG:(TFLAG:54):43
	CALL SET_HYPNO_TYPE(TFLAG:54)

CALL ACTION_FILTER(-1, 4, 989, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54, TFLAG:63)

@EXTRA_ACTION_989(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL


IF DA:ENEMY_ID:10 && DA:ENEMY_ID:20
	CALL ACTION_FILTER(-1, 17, 989, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54, TFLAG:63)
	CALL DAMAGE_COMMON((DA:ENEMY_ID:10), 2, 5000 + RAND:2500, 100)
	CALL ADD_ABL_EXP(13, (DA:ENEMY_ID:10), 6 + RAND:5)
	CALL DAMAGE_COMMON((DA:ENEMY_ID:20), 2, 5000 + RAND:2500, 100)
	CALL ADD_ABL_EXP(13, (DA:ENEMY_ID:20), 6 + RAND:5)
ENDIF
