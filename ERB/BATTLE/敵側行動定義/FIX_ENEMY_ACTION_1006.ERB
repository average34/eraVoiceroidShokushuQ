@FIX_ENEMY_ACTION_1006(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 5
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

IF GET_PENIS((DA:ENEMY_ID:51))
	TFLAG:58 = 10000 / TFLAG:57 + 44
ELSE
	TFLAG:58 = 10000 / TFLAG:57 + 20
ENDIF

IF GET_PENIS((DA:ENEMY_ID:51))
	TFLAG:64 = 22
ELSE
	TFLAG:64 = 10
ENDIF

IF GET_PENIS((DA:ENEMY_ID:51))
	TFLAG:65 = 30
ELSE
	TFLAG:65 = 20
ENDIF

TFLAG:61 = 150

TFLAG:59 = 0
TFLAG:91 = 0
TFLAG:99 = 0

TFLAG:63 = 3

CALL ACTION_FILTER(-1, 6, 1006, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54, TFLAG:63)

