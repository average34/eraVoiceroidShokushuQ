@FIX_ENEMY_ACTION_953(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 8
TFLAG:60 = 1


TFLAG:61 = 120

TFLAG:99 = 953

CALL ACTION_FILTER(-1, 8, 953, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54)



@EXTRA_ACTION_953(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率、侵食発生率

LOCAL:1 = 120

CALL GET_EQUIP_BONUS(CHARA_ID, 38)
IF RESULT
	LOCAL:1 = 0
ENDIF

FLAG:(FLAG:5 + 50) += 5 + RAND:5
IF RAND:100 < LOCAL:1
	BASE:CHARA_ID:5 += 8

	LOCAL:1 = 45
	;薬毒耐性、蓬莱人、異形耐性
	SIF TALENT:CHARA_ID:56
			LOCAL:1 -= 10
	SIF TALENT:CHARA_ID:128
			LOCAL:1 -= 15
	SIF TALENT:CHARA_ID:155
			LOCAL:1 -= 10

	IF RAND:100 < LOCAL:1
		CALL PRINT_MESSAGE(CHARA_ID, 445, 6, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID)
		CFLAG:CHARA_ID:80 += 1
		CALL ADD_ABL_EXP(14, (CHARA_ID), 5 + RAND:5)
	ENDIF
ENDIF