@FIX_ENEMY_ACTION_714(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 3
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

TFLAG:58 = 10000 / TFLAG:57 + 160 + 85 * DA:ENEMY_ID:3
TFLAG:60 = 1
TFLAG:61 = 150

TFLAG:64 = 50
TFLAG:65 = 40

TFLAG:59 = 0
TFLAG:90 = 520
TFLAG:91 = 0
TFLAG:92 = 0

TFLAG:63 = 2
TFLAG:99 = 714

CALL ACTION_FILTER(-1, 4, 714, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54, TFLAG:63)

@EXTRA_ACTION_714(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID

IF FLAG:522 != 2
	CALL ADD_BASE_NP(TFLAG:54,5+RAND:25)
	FLAG:99 ++
ENDIF
IF CFLAG:(TFLAG:54):28 == 0 && RAND:4 == 0
	CFLAG:(TFLAG:54):28 += 1
	FLAG:99 += 2
ENDIF

CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
FLAG:99 = 0
