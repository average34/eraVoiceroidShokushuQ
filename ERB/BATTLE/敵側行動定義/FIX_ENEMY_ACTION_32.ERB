@FIX_ENEMY_ACTION_32(ENEMY_ID)
#DIM ENEMY_ID

TFLAG:52 = ENEMY_ID
TFLAG:54 = DA:ENEMY_ID:51
TFLAG:55 = 6
TFLAG:60 = 1

TFLAG:57 = DA:ENEMY_ID:5

TFLAG:58 = 100
TFLAG:59 = 0

TFLAG:61 = 100

TFLAG:99 = 32

CALL ACTION_FILTER(-1, 7, 32, CALLNAME:(TFLAG:54), SAVESTR:ENEMY_ID, TFLAG:54)



@EXTRA_ACTION_32(ENEMY_ID, CHARA_ID)
#DIM ENEMY_ID
#DIM CHARA_ID
;LOCAL
;1	命中率

LOCAL:1 = 2000

IF TALENT:CHARA_ID:56
	LOCAL:1 = 80
ENDIF

IF RAND:100 < LOCAL:1 && (FLAG:10 || FLAG:11)
	CALL PRINT_MESSAGE(CHARA_ID, 17, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
	CFLAG:CHARA_ID:50 = 73
	IF FLAG:10 && (RAND:2 || !FLAG:11)
		CFLAG:CHARA_ID:51 = FLAG:10
	ELSE
		CFLAG:CHARA_ID:51 = FLAG:11
	ENDIF
	CFLAG:CHARA_ID:52 = 73
	CFLAG:CHARA_ID:54 = 0
ELSE
	CALL PRINT_MESSAGE(CHARA_ID, 27, TFLAG:99, CALLNAME:CHARA_ID, SAVESTR:ENEMY_ID, CHARA_ID)
ENDIF


